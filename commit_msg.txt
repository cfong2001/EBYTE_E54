🧹 [code health] Remove unused map_xy import in code_alien_style.py

🎯 What
Removed the unused `map_xy` function from the `shared.ui_utils` import on line 8 of `code_alien_style.py`.

💡 Why
`map_xy` is not used anywhere in the `code_alien_style.py` file. Removing it cleans up the namespace and improves readability.

✅ Verification
Ran `python3 test_all_modules.py --all` to ensure no functionality is broken. Tests passed.

✨ Result
Cleaner namespace without altering functionality.
